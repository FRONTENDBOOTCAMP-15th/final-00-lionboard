name: git push into another repo to deploy to vercel

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    container: pandoc/latex

    strategy:
      matrix:
        repo:
          - owner: uzoolove
            name: final-00-lionboard-release
            token: TOKEN_YONG
            email: EMAIL_YONG
          - owner: [fork한 레포지토리의 소유자]
            name: [fork한 레포지토리명]
            token: [시크릿 토큰을 저장한 변수]
            email: [시크릿 토큰 소유자 이메일을 저장한 변수]
            
    steps:
      - uses: actions/checkout@v2
      - name: Install mustache (to update the date)
        run: apk add ruby && gem install mustache
      - name: creates output
        run: sh ./build.sh
      - name: Pushes to another repository
        id: push_directory
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets[matrix.repo.token] }}
        with:
          source-directory: 'output'
          destination-github-username: ${{ matrix.repo.owner }}
          destination-repository-name: ${{ matrix.repo.name }}
          user-email: ${{ secrets[matrix.repo.email] }}
          commit-message: ${{ github.event.commits[0].message }}
          target-branch: main
      - name: Test get variable exported by push-to-another-repository
        run: echo $DESTINATION_CLONED_DIRECTORY